This is manual. 

____________________________________________________________________________________________________________________
first step: 

to compile (aka update to newest runnable version): 
javac Main.java IntoFile.java Img.java Predefined.java Stat.java Transcribe.java

to run normal editor: 
java Main

_____________________________________________________________________________________________________________________
. runnables:  Main, Stat, Transcribe

Main  =  main Program. Allows you to edit on a picture, which will be saved into the file named "drawn.png"
Stat  =  Program which will increase the size of each bit inside "drawn.png" by some value 
Transcribe =  Program which prints the red bits inside "template.png"  onto  "drawn.png"

(to run each, do 
java <name>
 inside console)
_____________________________________________________________________________________________________________________

Flags:      

Main -q          // -q starts immediately and ignores others : random color, new file of template, no autofill
Main -o          // setting this flag will run the old file (drawn.png). Not setting this means the old file gets lost.  
Main -a          // setting -a will enable autofill
Main -c <number> // setting -c number will set the Color  . . . NUMBER MUST BE IN HEXADECIMAL WITHOUT THE LEADING 0x
Main -b <number> // setting the brushsize.  Brushsize = a square around the point you click. 

Main default before using any flags: new file, random color, no autofill, brush size = 10


Stat -s <number> // will increase size of each bit in the picture by <number> times 
Stat default: 5

Transcribe -c <number> // number should be the rgb number in Hexadecimal form of the color. It will map all bits inside "template.png" onto "drawn.png"
Transcribe -v          // if set, the color of each bit affected inside the "drawn.png" will be inversed.
                       // If not set, then will map original color (specified in -c or red)
Transcribe default: maps the red (FF0000) bits of "template.png" . will be same color on "default.png" as well. 
____________________________________________________________________________________________________________________

examples: 

java Main

java Main -q

java Main -o -c FF00F0 -a -b 20

java Main -o 

java Main -c FF00F0 -b 3

java Transcribe 

java Transcribe -c FFFFFF

java Transcribe -c FFFFFF -v

java Stat 

java Stat -s 2

